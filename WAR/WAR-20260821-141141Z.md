# Weekly Action Report — 2026-08-21

Repo: `troydam/gitLab-roc` · Branch: `claude/gitlab-roc-ansible-setup-xvfu3b`

## Summary

Built out a generic GitLab CI + Ansible multi-environment deployment
pipeline template (dev → preprod → prod, db → app → web tiers, 3-wave
canary rollout per tier), fully gated behind manual human approval,
and documented it for future contributors. Also reviewed a second,
unrelated reference pipeline (`notes/.gitlab-ci-ref.yml`) for
enterprise-practice gaps.

## Completed this session

- Scaffolded initial repo structure: `.gitlab-ci.yml`, `site.yml`,
  per-environment `inventory/<env>/hosts.yml` +
  `group_vars/<env>_<tier>.yml`.
- Fixed inventory/CI mismatches found in review: removed duplicated
  `vars:` blocks from `hosts.yml` (group_vars is the single source of
  truth), fixed `db_host` templating that referenced a removed
  `ansible_host` key, resolved a `web_port` conflict.
- Fixed `site.yml` host targeting — plays now match real inventory
  groups (standard Ansible parent/child group pattern) instead of an
  ineffective `hosts: db/app/web` that never matched anything.
- Restricted the pipeline to the `main` branch via `workflow: rules`.
- Added `roles/db`, `roles/app`, `roles/web` as placeholder skeletons
  (stack-agnostic template — real tasks pending a stack decision).
- Added `ansible.cfg` and a `lint` CI stage (yamllint + ansible-lint)
  ahead of `dev`.
- Restored/added the SSH-agent + Ansible image setup in
  `.deploy-template` so jobs can actually run against real hosts.
- Redesigned the rollout model: each tier now deploys in 3 explicit
  waves (`canary` → `wave2` → `final`), each wave a named inventory
  group, each wave a **fully manual** CI job (including canary) — no
  step past `lint` runs without a human click. Verified the resulting
  27-job dependency graph (no cycles, no dangling `needs:`, correct
  tier/env handoffs) programmatically.
- Wrote `CLAUDE.md`: wave-rollout convention, gating rules, gate
  discipline, and a single prioritized Shelf of deferred work (real
  role tasks, secrets/Vault convention, protected environments,
  per-gate check guidance, smoke testing, dynamic tier list,
  `requirements.yml`).
- Wrote `docs/pipeline.md` (Mermaid, renders natively on
  GitLab/GitHub — the source of truth) and
  `docs/wave-rollout-pipeline.html` (hand-authored SVG, light/dark
  themed, generated presentation copy of the same content).
- Reviewed `notes/.gitlab-ci-ref.yml` (a separate, real-world AWX-
  driven pipeline) for enterprise practice; wrote findings and a job
  categorization to `notes/gitlab-ci-ref-shelf.md`, tracked separately
  from this project's own Shelf.

## Open items (see `CLAUDE.md` → Shelf for the authoritative, prioritized list)

1. Real role tasks (blocks meaningful use of the pipeline)
2. Secrets convention (Vault vs CI/CD variables)
3. Protected environments (restrict who can click prod/preprod gates)
4. Per-gate "what to check" guidance
5. Smoke-test / post-deploy verification
6. Dynamic tier list (child-pipeline generation, if a second project
   ever needs a different tier list than db/app/web)
7. `requirements.yml`

Separately, `notes/gitlab-ci-ref-shelf.md` tracks unaddressed findings
in the *other* reviewed pipeline (not this repo's own) — most urgent:
a hardcoded apply-mode default that contradicts its own documented
dry-run-by-default safety rule, and an ungated preprod deploy path.

## Not yet done / explicitly out of scope for now

- No real GitLab instance has run this pipeline — repo lives on
  GitHub in this session; `.gitlab-ci.yml` has not been triggered
  anywhere.
- No `main` branch exists yet / no merge performed — all work is on
  `claude/gitlab-roc-ansible-setup-xvfu3b`.
