---
# Placeholder task set for the db-tier role.
# Replace with real tasks (install engine, create db/user, tune config)
# once the target database technology is decided. Available vars are
# defined in inventory/<env>/group_vars/<env>_db.yml (db_port, db_name,
# db_user, db_max_connections, postgres_version, etc.).

- name: db role placeholder
  ansible.builtin.debug:
    msg: >-
      db role not yet implemented for {{ inventory_hostname }}
      (env={{ env }}, tier={{ tier }})
